mutation Login($input: LoginRequestDto!) {
    login(input: $input) {
        id
        firstName
        lastName
        orgName
        username
        avatarUrl
        accountType
        role
        organization
        staffs
        phoneNumber
        accessToken
        displayName
        isFirstLogin
    }
}
