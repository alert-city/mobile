mutation CreateUser($input: UserRequestDto!) {
    createUser(input: $input) {
        id
        firstName
        lastName
        orgName
        username
        displayName
        accountType
        role
        organization
        staffs
        mobilePhone
        avatarUrl
        is2FAEnabled
        isAccountActivated
        accessToken
        refreshToken
    }
}
